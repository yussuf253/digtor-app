import 'package:flutter/material.dart';

class AppColors {
  // Couleurs principales
  static const Color primary = Color(0xFF2F80ED);
  static const Color secondary = Color(0xFF4894FE);

  // Couleurs de fond
  static const Color background = Color(0xFFFAFAFA);
  static const Color cardBackground = Colors.white;
  static const Color surfaceVariant = Color(0xFFF3F8FF);

  // Couleurs de texte
  static const Color text = Color(0xFF1A1C1E);
  static const Color textLight = Color(0xFF6C7072);
  static const Color textPrimary = Color(0xFF1A1C1E);
  static const Color textSecondary = Color(0xFF6C7072);
  static const Color textTertiary = Color(0xFF9EA1A3);

  // Couleurs d'accentuation
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFA726);
  static const Color error = Color(0xFFEF5350);
  static const Color info = Color(0xFF2196F3);

  // Couleur étoile
  static const Color star = Color(0xFFFFB800);

  // Couleurs des états
  static const Color online = Color(0xFF4CAF50);
  static const Color offline = Color(0xFFBDBDBD);

  // Couleurs des ombres
  static Color shadowColor = Colors.black.withOpacity(0.1);
}
